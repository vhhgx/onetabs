# OneTabs V2.0 架构说明

> 创建时间：2026-01-20  
> 最后更新：2026-01-26  
> 状态：已实现

---

## 📐 整体架构

### 当前实现的页面结构

OneTabs V2.0 采用 **侧边栏 + 主内容区 + 底部Tab** 的布局模式：

```
┌───────────────────────────────────────────────────────────────────┐
│  HomeView.vue                                                      │
│  ┌──────────────┬──────────────────────────────────────────────┐  │
│  │              │  主内容区 (可变宽度)                          │  │
│  │  SessionsView│  ┌─────────────────────────────────────────┐ │  │
│  │  (侧边栏)    │  │                                         │ │  │
│  │  宽度:320px  │◀─┤  拖拽手柄                               │ │  │
│  │  可折叠      │  │                                         │ │  │
│  │              │  │  ┌───────────────────────────────────┐ │ │  │
│  │  📦 会话列表 │  │  │  Tab内容区 (v-if切换)             │ │ │  │
│  │  - SessionCard│  │  │  - CollectionsView (收藏集)      │ │ │  │
│  │  - TabItem   │  │  │  - TemplatesView (模板)           │ │ │  │
│  │              │  │  │  - BookmarksView (书签) [默认]    │ │ │  │
│  │              │  │  └───────────────────────────────────┘ │ │  │
│  │              │  │                                         │ │  │
│  │              │  │  ┌───────────────────────────────────┐ │ │  │
│  │              │  │  │  底部Tab导航栏 (48px)              │ │ │  │
│  │              │  │  │  [Collections] [Templates] [Bookmarks]│ │  │
│  │              │  │  └───────────────────────────────────┘ │ │  │
│  │              │  └─────────────────────────────────────────┘ │  │
│  └──────────────┴──────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────────┘
```

### 关键特性

| 特性 | 实现方式 |
|------|----------|
| 侧边栏展开/收起 | 拖拽手柄 + 点击切换，过渡动画 0.3s |
| Tab切换 | `v-if` 条件渲染，`activeTab` 状态控制 |
| 侧边栏固定内容 | SessionsView 始终渲染，只改变容器宽度 |
| 主题支持 | CSS变量 `--bg-active`, `--text-primary` 等 |

---

## 🗂️ 文件结构

### 核心文件清单

```
src/
├── App.vue                    # 应用根组件，router-view
├── main.js                    # 入口文件，Pinia/Router/PrimeVue初始化
│
├── router/
│   └── index.js               # 路由配置 (/ 和 /settings)
│
├── views/
│   ├── HomeView.vue           # ★ 主页面布局容器
│   ├── SessionsView.vue       # 会话收纳视图 (左侧边栏)
│   ├── CollectionsView.vue    # 收藏集视图 (Tab内容)
│   ├── TemplatesView.vue      # 窗口模板视图 (Tab内容)
│   ├── BookmarksView.vue      # 书签视图 (Tab内容，默认)
│   └── Settings.vue           # 设置页面 (独立路由)
│
├── components/
│   ├── SessionCard.vue        # 会话卡片
│   ├── TabItem.vue            # 标签页项
│   ├── CollectionCard.vue     # 收藏集卡片
│   ├── CollectionEditor.vue   # 收藏集编辑弹窗
│   ├── TemplateCard.vue       # 模板卡片
│   ├── TemplateEditor.vue     # 模板编辑弹窗
│   ├── TabSelector.vue        # 标签页选择器弹窗
│   ├── CollectionSelector.vue # 收藏集选择器弹窗
│   ├── SearchBar.vue          # 全局搜索组件
│   ├── ContextMenu.vue        # 右键菜单
│   ├── DropZone.vue           # 拖放目标区域
│   ├── DraggableTab.vue       # 可拖拽标签
│   ├── DragPreview.vue        # 拖拽预览
│   ├── EmptyState.vue         # 空状态提示
│   └── ShortcutsHelp.vue      # 快捷键帮助
│
├── stores/
│   ├── sessionsStore.js       # 会话数据 (tabGroups)
│   ├── collectionsStore.js    # 收藏集数据 (onetabs_collections)
│   ├── templatesStore.js      # 模板数据 (onetabs_templates)
│   ├── settingsStore.js       # 设置数据 (onetabs_settings)
│   ├── themeStore.js          # 主题数据 (onetabs_theme)
│   └── bookmarksStore.js      # 书签数据
│
├── utils/
│   ├── chrome-storage.js      # Chrome存储封装
│   ├── shortcuts.js           # 快捷键配置
│   ├── contextMenus.js        # 右键菜单配置
│   └── errorHandler.js        # 错误处理
│
└── assets/
    ├── background.js          # Service Worker
    ├── manifest.json          # 扩展配置
    └── style/                 # 样式文件
```

---

## 🎨 UI组件层级关系

### HomeView 布局逻辑

```
HomeView 布局结构:
┌──────────────────────────────────────────────────────────┐
│ container-block (flex布局, 100%宽高)                     │
│ ┌────────────────┬─────────────────────────────────────┐ │
│ │ 左侧边栏       │ 右侧主区域                          │ │
│ │ width:         │ ┌─────────────────────────────────┐ │ │
│ │ isExpanded     │ │ 拖拽手柄 (点击/拖拽切换侧边栏)  │ │ │
│ │ ? 320px : 0    │ ├─────────────────────────────────┤ │ │
│ │                │ │ Tab内容区 (v-if切换)            │ │ │
│ │ SessionsView   │ │ - activeTab='collections'       │ │ │
│ │ (始终渲染)     │ │ - activeTab='templates'         │ │ │
│ │                │ │ - activeTab='bookmarks' (默认)  │ │ │
│ │                │ ├─────────────────────────────────┤ │ │
│ │                │ │ 底部Tab栏 (48px)                │ │ │
│ │                │ │ [Collections][Templates][Bookmarks]│ │
│ └────────────────┴─────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────┘

状态管理:
- isExpanded: boolean     // 侧边栏展开状态
- activeTab: string       // 当前Tab: 'collections'|'templates'|'bookmarks'
- isDragging: boolean     // 拖拽手柄状态

交互逻辑:
- 拖拽手柄: mousedown → 记录起始位置 → mousemove判断方向 → 展开/收起
- 拖拽距离 > 50px 触发切换
- 拖拽距离 < 50px 视为点击，toggle展开状态
```

### 组件依赖关系图

```
HomeView.vue
├── SessionsView.vue
│   ├── SessionCard.vue
│   │   ├── TabItem.vue (可拖拽)
│   │   └── ContextMenu.vue
│   └── EmptyState.vue
│
├── CollectionsView.vue
│   ├── CollectionCard.vue
│   │   └── TabItem.vue
│   ├── CollectionEditor.vue
│   │   ├── TabSelector.vue
│   │   └── 颜色选择器
│   ├── DropZone.vue
│   └── EmptyState.vue
│
├── TemplatesView.vue
│   ├── TemplateCard.vue
│   │   └── 收藏集预览
│   ├── TemplateEditor.vue
│   │   ├── CollectionSelector.vue
│   │   └── TabSelector.vue
│   ├── DropZone.vue
│   └── EmptyState.vue
│
└── BookmarksView.vue
    ├── 书签分类
    └── 书签项
```

---

## 🔄 路由设计

### 当前路由配置

```javascript
// router/index.js
const routes = [
  {
    path: '/',
    name: 'Home',
    component: HomeView,  // 主页面，包含侧边栏+Tab切换
    meta: { title: 'OneTabs', keepAlive: true }
  },
  {
    path: '/settings',
    name: 'Settings',
    component: Settings,  // 独立设置页面
    meta: { title: 'OneTabs - 设置' }
  },
  {
    path: '/:pathMatch(.*)*',
    redirect: '/'  // 未知路径重定向
  }
]
```

### 架构决策说明

✅ **当前方案：侧边栏 + Tab切换**
- SessionsView 作为固定侧边栏，可折叠
- Collections/Templates/Bookmarks 通过底部Tab切换
- 状态通过 Pinia Store 管理，切换时保持

❌ **未采用的方案：完全路由分离**
- 原因：Chrome扩展刷新会丢失路由状态
- 原因：切换体验不如Tab流畅

---

## 🗄️ 状态管理 (Pinia Stores)

### Store 架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Chrome Storage                            │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐  │
│  │  tabGroups   │onetabs_      │onetabs_      │onetabs_      │  │
│  │  (会话)      │collections   │templates     │settings      │  │
│  └──────┬───────┴──────┬───────┴──────┬───────┴──────┬───────┘  │
└─────────┼──────────────┼──────────────┼──────────────┼──────────┘
          ↓              ↓              ↓              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        Pinia Stores                              │
│  ┌──────────────┬──────────────┬──────────────┬──────────────┐  │
│  │sessionsStore │collections   │templates     │settings      │  │
│  │              │Store         │Store         │Store         │  │
│  ├──────────────┼──────────────┼──────────────┼──────────────┤  │
│  │sessions[]    │collections[] │templates[]   │autoClose     │  │
│  │isLoading     │isLoading     │isLoading     │keepPinned    │  │
│  │              │currentColl   │currentTpl    │maxSessions   │  │
│  └──────────────┴──────────────┴──────────────┴──────────────┘  │
└─────────────────────────────────────────────────────────────────┘
          ↓              ↓              ↓              ↓
┌─────────────────────────────────────────────────────────────────┐
│                        Vue Components                            │
│  SessionsView   CollectionsView  TemplatesView   Settings       │
└─────────────────────────────────────────────────────────────────┘
```

### 各 Store 详细说明

#### sessionsStore.js
```javascript
// 存储键: tabGroups
state: {
  sessions: [],          // 会话列表 (按时间排序)
  currentSession: null,
  isLoading: false,
  lastLoaded: null
}

// 主要 Actions
loadSessions()           // 从 Storage 加载
saveSession(data)        // 保存新会话
deleteSession(id)        // 删除会话
togglePinSession(id)     // 置顶/取消置顶
restoreSession(id)       // 恢复整个会话
restoreTab(url)          // 恢复单个标签页
```

#### collectionsStore.js
```javascript
// 存储键: onetabs_collections
state: {
  collections: [],       // 收藏集列表
  currentCollection: null,
  isLoading: false
}

// 主要 Actions
loadCollections()        // 加载收藏集
createCollection(data)   // 创建收藏集
updateCollection(id, data)  // 更新收藏集
deleteCollection(id)     // 删除收藏集
addTab(collectionId, tab)   // 添加标签页
removeTab(collectionId, tabIndex)  // 移除标签页
openCollection(id, options)  // 打开收藏集
```

#### templatesStore.js
```javascript
// 存储键: onetabs_templates
state: {
  templates: [],         // 模板列表
  currentTemplate: null,
  isLoading: false
}

// 主要 Actions
loadTemplates()          // 加载模板
createTemplate(data)     // 创建模板
createFromCurrentWindow(name, desc)  // 从当前窗口创建
updateTemplate(id, data) // 更新模板
deleteTemplate(id)       // 删除模板
duplicateTemplate(id)    // 复制模板
openTemplate(id)         // 打开模板
```

#### settingsStore.js
```javascript
// 存储键: onetabs_settings
state: {
  autoClose: true,       // 收纳后自动关闭
  keepPinned: false,     // 保留固定标签页
  maxSessions: 50,       // 最大会话数
  enableDrag: true,      // 启用拖拽
  removeAfterDrag: false, // 拖拽后移除源
  showDragPreview: true  // 显示拖拽预览
}
```

#### themeStore.js
```javascript
// 存储键: onetabs_theme
state: {
  currentTheme: 'light', // 'light' | 'dark'
  followSystem: false,   // 跟随系统
  systemPreference: 'light'
}

// 主要 Actions
setTheme(theme)          // 设置主题
setFollowSystem(value)   // 设置跟随系统
applyTheme()             // 应用主题到DOM
```

---

## 🎨 UI组件详解

### SessionCard 组件

会话卡片，展示保存的标签页组。

```
结构:
┌─────────────────────────────────────────────────────┐
│ card-header (点击展开/收起, 右键菜单)               │
│ ┌──────┬────────────────────────┬────────────────┐ │
│ │ 颜色 │ 标题 + 元信息          │ 操作按钮       │ │
│ │ 指示 │ "工作" · 5分钟前 · 4个 │ 📌 恢复 删除   │ │
│ └──────┴────────────────────────┴────────────────┘ │
├─────────────────────────────────────────────────────┤
│ card-body (v-show="isExpanded")                     │
│ ├── TabItem (draggable, 可删除, 可添加到收藏集)    │
│ ├── TabItem                                         │
│ └── TabItem                                         │
└─────────────────────────────────────────────────────┘

Props: session (Object)
Events: @restore, @delete, @toggle-pin

内部状态:
- isExpanded: boolean (默认true)
- showContextMenu: boolean
- contextMenuPosition: {x, y}

交互逻辑:
- 点击header → 切换展开状态
- 右键header → 显示上下文菜单
- 点击恢复 → emit('restore', session.date)
- 点击删除 → 确认弹窗 → emit('delete', session.date)
```

### CollectionCard 组件

收藏集卡片，展示用户创建的标签页组。

```
结构:
┌─────────────────────────────────────────────────────┐
│ card-header                                         │
│ ┌──────┬────────────────────────┬────────────────┐ │
│ │ 颜色 │ 名称 + 标签数          │ 操作按钮       │ │
│ │ 圆点 │ "开发资源" · 8个标签页 │ ⭐ 开 编 删    │ │
│ └──────┴────────────────────────┴────────────────┘ │
├─────────────────────────────────────────────────────┤
│ card-body (可折叠)                                  │
│ ├── tab-item (点击打开, 可删除)                    │
│ ├── tab-item                                        │
│ └── tab-item                                        │
└─────────────────────────────────────────────────────┘

Props: collection (Object)
Events: @pin, @open, @open-current, @edit, @delete, @duplicate, @add-to-template, @remove-tab

交互逻辑:
- 打开 → chrome.tabs.create批量创建 + chrome.tabGroups.group创建分组
- 编辑 → 打开CollectionEditor弹窗
- 删除 → 确认弹窗 → collectionsStore.deleteCollection()
- 右键 → 显示上下文菜单
```

### TemplateCard 组件

模板卡片，展示窗口模板。

```
结构:
┌─────────────────────────────────────────────────────┐
│ card-header                                         │
│ ┌──────┬────────────────────────┬────────────────┐ │
│ │ 图标 │ 名称 + 描述            │ 操作按钮       │ │
│ │ 📋   │ "工作环境"             │ 开 编 复 删    │ │
│ │      │ 2个收藏集 · 15个标签页 │                │ │
│ └──────┴────────────────────────┴────────────────┘ │
├─────────────────────────────────────────────────────┤
│ card-body (可折叠)                                  │
│ ├── 收藏集预览                                     │
│ │   ┌───────────────────────────────────────────┐ │
│ │   │ [蓝] 开发资源 (5) [引用]                  │ │
│ │   │ [红] 设计参考 (3) [快照]                  │ │
│ │   └───────────────────────────────────────────┘ │
│ └── 独立标签页预览                                 │
│     ├── tab-preview                                │
│     └── tab-preview                                │
└─────────────────────────────────────────────────────┘

Props: template (Object)
Events: @open, @edit, @duplicate, @delete

交互逻辑:
- 打开 → chrome.windows.create创建新窗口 → 遍历collections创建标签页和分组
- 引用模式 → 从collectionsStore获取最新数据
- 快照模式 → 使用template.collections中保存的数据
- 复制 → 克隆数据 + 新ID + 名称加"(副本)"
```

### Editor 弹窗组件

CollectionEditor 和 TemplateEditor 共用的模式。

```
CollectionEditor:
┌────────────────────────────────────────────────────────┐
│ [创建/编辑收藏集]                              [X]    │
├────────────────────────────────────────────────────────┤
│ 收藏集名称: [________________]                        │
│                                                        │
│ 标签颜色:  ● ● ● ● ● ● ● ● ●  (9种Chrome颜色)        │
│                                                        │
│ 标签页 (3):                    [从窗口选择] [手动添加] │
│ ┌────────────────────────────────────────────────────┐│
│ │ ≡  🌐 GitHub - xxx               [×]              ││
│ │ ≡  🌐 Stack Overflow - xxx       [×]              ││
│ │ ≡  🌐 MDN Web Docs               [×]              ││
│ └────────────────────────────────────────────────────┘│
├────────────────────────────────────────────────────────┤
│                              [取消]  [保存]           │
└────────────────────────────────────────────────────────┘

验证规则:
- 名称不能为空
- 至少1个标签页

TemplateEditor:
┌────────────────────────────────────────────────────────┐
│ [创建/编辑窗口模板]                            [X]    │
├────────────────────────────────────────────────────────┤
│ 模板名称: [________________]                          │
│ 描述:     [________________]                          │
│                                                        │
│ 创建方式: [从当前窗口] [手动创建]                     │
│                                                        │
│ 标签页组 (2):                        [添加收藏集]     │
│ ┌────────────────────────────────────────────────────┐│
│ │ ≡ [蓝] 开发资源 (5) [引用] ☑创建分组        [×]   ││
│ │ ≡ [红] 设计参考 (3) [快照] ☑创建分组        [×]   ││
│ └────────────────────────────────────────────────────┘│
│                                                        │
│ 独立标签页 (1):                      [添加标签页]     │
│ ┌────────────────────────────────────────────────────┐│
│ │ 🌐 Google ☐固定                              [×]  ││
│ └────────────────────────────────────────────────────┘│
├────────────────────────────────────────────────────────┤
│                              [取消]  [保存]           │
└────────────────────────────────────────────────────────┘
```

---

## 🔧 核心交互实现

### 拖拽系统

```
┌─────────────────────────────────────────────────────────────┐
│  拖拽源                              拖拽目标                │
│  ┌─────────────────┐                ┌─────────────────┐     │
│  │  TabItem        │  ───拖拽───▶   │  DropZone       │     │
│  │  (draggable)    │                │  (collection/   │     │
│  │                 │                │   template)     │     │
│  └─────────────────┘                └─────────────────┘     │
│        │                                   │                 │
│        ▼                                   ▼                 │
│  DragPreview                          视觉反馈               │
│  (跟随鼠标)                           (边框高亮)            │
└─────────────────────────────────────────────────────────────┘
```

**拖拽数据格式:**
```javascript
{
  tab: { url, title, favIconUrl },
  sourceType: 'session' | 'collection' | 'template',
  sourceId: string
}
```

### 右键菜单系统

```javascript
// utils/contextMenus.js
export const getSessionContextMenu = (session) => [
  { id: 'restore', label: '恢复全部', icon: 'pi pi-refresh' },
  { id: 'pin', label: session.isPinned ? '取消置顶' : '置顶' },
  { divider: true },
  { id: 'delete', label: '删除', danger: true }
]

export const getTabContextMenu = (tab) => [
  { id: 'open', label: '打开' },
  { id: 'copy-url', label: '复制链接' },
  { id: 'add-to-collection', label: '添加到收藏集' },
  { id: 'add-to-template', label: '添加到模板' },
  { divider: true },
  { id: 'delete', label: '删除', danger: true }
]
```

### 快捷键系统

```javascript
// utils/shortcuts.js
export const shortcuts = {
  FOCUS_SEARCH: { key: 'ctrl+f', mac: 'cmd+f' },
  OPEN_SETTINGS: { key: 'ctrl+,', mac: 'cmd+,' },
  TAB_SESSIONS: { key: 'ctrl+1', mac: 'cmd+1' },
  TAB_COLLECTIONS: { key: 'ctrl+2', mac: 'cmd+2' },
  TAB_TEMPLATES: { key: 'ctrl+3', mac: 'cmd+3' },
  CLOSE_DIALOG: { key: 'esc' }
}
```

---

## 📦 数据结构

### Session (会话)

```javascript
{
  date: number,              // 时间戳，作为唯一ID
  type: 'grouped' | 'ungrouped',
  title: string,             // 会话标题
  isPinned: boolean,         // 是否置顶
  groupInfo: {               // 仅 grouped 类型
    id: number,
    title: string,
    color: string,           // Chrome Tab Group颜色
    collapsed: boolean
  },
  tabs: [
    {
      url: string,
      title: string,
      favIconUrl: string,
      groupId: number
    }
  ]
}
```

### Collection (收藏集)

```javascript
{
  id: string,                // 唯一ID: `collection_${timestamp}_${random}`
  name: string,              // 收藏集名称
  color: string,             // 颜色: grey/blue/red/yellow/green/pink/purple/cyan/orange
  icon: string,              // 图标 emoji
  pinned: boolean,           // 是否置顶
  createdAt: number,
  updatedAt: number,
  tabs: [
    {
      id: string,
      url: string,
      title: string,
      favIconUrl: string,
      order: number
    }
  ]
}
```

### Template (窗口模板)

```javascript
{
  id: string,                // 唯一ID: `template_${timestamp}_${random}`
  name: string,              // 模板名称
  description: string,       // 模板描述
  icon: string,              // 图标 emoji
  createdAt: number,
  updatedAt: number,
  collections: [             // 包含的收藏集
    {
      id: string,            // 收藏集ID或临时ID
      collectionId: string,  // 引用的收藏集ID (引用模式)
      name: string,
      color: string,
      isReference: boolean,  // true=引用模式, false=快照模式
      createGroup: boolean,  // 打开时是否创建Tab Group
      tabs: [...]            // 快照模式时的标签页数据
    }
  ],
  standaloneTabs: [          // 独立标签页
    {
      id: string,
      url: string,
      title: string,
      favIconUrl: string,
      pinned: boolean        // 是否固定标签页
    }
  ]
}
```

---

## 🎯 Chrome API 使用

### background.js 核心逻辑

```javascript
// 1. 监听扩展图标点击
chrome.action.onClicked.addListener(async () => {
  await saveTabs()
})

// 2. 保存标签页流程
async function saveTabs() {
  // 获取设置
  const settings = await chrome.storage.local.get(['onetabs_settings'])
  
  // 获取当前窗口
  const currentWindow = await chrome.windows.getCurrent()
  
  // 获取所有标签页
  const tabs = await chrome.tabs.query({ windowId: currentWindow.id })
  
  // 获取标签组信息
  const tabGroups = await chrome.tabGroups.query({ windowId: currentWindow.id })
  
  // 组织数据并保存
  await chrome.storage.local.set({ tabGroups: organizedData })
  
  // 打开扩展页面
  await chrome.tabs.create({ url: chrome.runtime.getURL('index.html') })
  
  // 关闭其他标签页
  for (const tab of tabs) {
    if (shouldClose(tab)) {
      await chrome.tabs.remove(tab.id)
    }
  }
}
```

### 恢复标签页

```javascript
// 恢复单个标签页
await chrome.tabs.create({ url: tab.url })

// 恢复标签组
const tabIds = []
for (const tab of collection.tabs) {
  const newTab = await chrome.tabs.create({ url: tab.url })
  tabIds.push(newTab.id)
}
const groupId = await chrome.tabs.group({ tabIds })
await chrome.tabGroups.update(groupId, {
  title: collection.name,
  color: collection.color
})

// 在新窗口打开模板
const newWindow = await chrome.windows.create({ url: urls })
```

---

## 🎨 主题系统

### CSS变量

```css
:root {
  /* 背景色 */
  --bg-primary: #ffffff;
  --bg-secondary: #f5f5f7;
  --bg-active: #ffffff;
  
  /* 文字色 */
  --text-primary: #1d1d1f;
  --text-secondary: #86868b;
  
  /* 边框 */
  --border-color: rgba(0, 0, 0, 0.1);
  
  /* 主题色 */
  --primary-color: #007aff;
}

[data-theme="dark"] {
  --bg-primary: #1c1c1e;
  --bg-secondary: #2c2c2e;
  --text-primary: #ffffff;
  --text-secondary: #98989d;
}
```

### 颜色映射 (Chrome Tab Group颜色)

```javascript
const colorMap = {
  grey: '#5f6368',
  blue: '#1a73e8',
  red: '#d93025',
  yellow: '#f9ab00',
  green: '#1e8e3e',
  pink: '#d01884',
  purple: '#a142f4',
  cyan: '#007b83',
  orange: '#fa903e'
}
```

---

## 📦 组件通信模式

### Props Down, Events Up

```
┌─────────────────────────────────────────────┐
│  Parent (SessionsView)                       │
│    │                                         │
│    ├── props: session ─────────▶ SessionCard │
│    │                                         │
│    ◀── @restore ──────────────── SessionCard │
│    ◀── @delete ───────────────── SessionCard │
│    ◀── @toggle-pin ───────────── SessionCard │
│                                              │
└─────────────────────────────────────────────┘
```

### Store 注入模式

```javascript
// 在组件中
import { useSessionsStore } from '@/stores/sessionsStore'
import { useToast } from 'primevue/usetoast'

const sessionsStore = useSessionsStore()
const toast = useToast()

// 直接调用 store action
await sessionsStore.deleteSession(sessionId)
toast.add({ summary: '删除成功', severity: 'success' })
```

---

## ✅ 开发状态总结

| 模块 | 状态 | 说明 |
|------|------|------|
| 布局框架 | ✅ 完成 | HomeView 侧边栏+Tab布局 |
| 会话收纳 | ✅ 完成 | SessionsView, SessionCard |
| 收藏集管理 | ✅ 完成 | CollectionsView, CollectionEditor |
| 模板管理 | ✅ 完成 | TemplatesView, TemplateEditor |
| 拖拽系统 | ✅ 完成 | DraggableTab, DropZone, DragPreview |
| 搜索功能 | ✅ 完成 | SearchBar 全局搜索 |
| 右键菜单 | ✅ 完成 | ContextMenu 组件 |
| 主题切换 | ✅ 完成 | themeStore, 亮/暗/跟随系统 |
| 快捷键 | ✅ 完成 | shortcuts.js |
| 设置页面 | ✅ 完成 | Settings.vue |
| 错误处理 | ✅ 完成 | errorHandler.js |

---

## 📚 快速开始指南

### 1. 添加新的视图

```javascript
// 1. 创建 views/NewView.vue
// 2. 在 HomeView.vue 中添加 Tab
<NewView v-else-if="activeTab === 'new'" />

// 3. 添加 Tab 按钮
<button @click="activeTab = 'new'">New</button>
```

### 2. 添加新的 Store

```javascript
// 1. 创建 stores/newStore.js
import { defineStore } from 'pinia'
import { chromeStorageGet, chromeStorageSet } from '@/utils/chrome-storage'

export const useNewStore = defineStore('new', {
  state: () => ({ items: [] }),
  actions: {
    async loadItems() {
      const data = await chromeStorageGet('onetabs_new')
      this.items = data?.onetabs_new || []
    }
  }
})
```

### 3. 添加右键菜单

```javascript
// 1. 在 utils/contextMenus.js 添加菜单配置
export const getNewContextMenu = (item) => [
  { id: 'action1', label: '操作1', icon: 'pi pi-xxx' },
  { divider: true },
  { id: 'delete', label: '删除', danger: true }
]

// 2. 在组件中使用
import { useContextMenu } from '@/composables/useContextMenu'
const { showContextMenu, contextMenuPosition, showMenu } = useContextMenu()
```

---

## 🔄 更新记录

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-01-26 | 2.0 | 更新为实际实现的架构，添加详细组件说明 |
| 2026-01-20 | 1.0 | 确定单页面+Tab切换架构 |
